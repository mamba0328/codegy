import { Poppins, Inter, Chivo } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const poppins = Poppins({weight:['400', '700'], subsets: ['latin'] });
export const chivo = Chivo({weight:['400', '700'], subsets: ['latin'] });